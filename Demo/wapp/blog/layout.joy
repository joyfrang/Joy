bring /wapp/html.joy

// As the filename is already layout.joy, the function name can be anything
#parent(HtmlShell) // The #parent tag can be used to override the layout's parent layout
// #parent(noth) // Also can be set to noth so there's no parent layout
Layout BlogLayout() {
    //...
    return {children}
}