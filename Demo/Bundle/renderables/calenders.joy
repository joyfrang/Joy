expose View Calender() { ~> an exposed renderable can be used from outside the bundle
    return <PrivateCalender/>
}

Island PrivateCalender() { ~> internal and unexposed — only usable within this file
    return <span>...</span>
}
