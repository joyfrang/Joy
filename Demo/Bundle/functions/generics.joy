thing Pair<A, B> {
    Pair(A first, B second)
}

impl Pair<A, B> {
    Pair<B, A> swap(Pair<A, B> self) {
        return (
            first: self.second,
            second: self.first
        )
    }
}

noth functionThatExists(Eatable food, bit fastOrSlow) {
    implementation here <~
}