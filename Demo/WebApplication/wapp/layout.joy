// bring can be used to import a file, so its exposed functions can be used from here
bring ./html.joy

#parent(html.HtmlShell)
Layout MainLayout(Renderable children) {
    return <div>{children}</div>
}